bundle:
  name: midas

sync:
  include:
    - .build

artifacts:
  default:
    build: apx build

resources:
  apps:
    midas-app:
      name: "midas"
      description: "midas created with apx"
      source_code_path: ./.build

targets:
  dev:
    mode: development
    default: true
